[package]
name = "coulomb-validate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Runs a scenario through coulomb-core and the Python reference, compares acceptance, energy, and particle count."

[dependencies]
coulomb-core = { path = "../coulomb-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
